import "assert" as assert

let fib n = {
  ((n != 0) && (n != 1) && (n > 0))? {
    fib (n-1) + fib (n-2)
  } : {
    n
  }
}

assert ((fib 10) == 55) "shits broken"
assert ((fib 20) == 6765) "shits broken"
print (fib 10) // should be 55
print (fib 20) // should be 6765
